/* eslint-disable react/jsx-filename-extension */
import React from 'react';
import '@testing-library/jest-dom';
import userEvent from '@testing-library/user-event';
import { render, screen } from '@testing-library/react';
import ByDateTab from '../components/ByDateTab';

describe('by dates compnents are rendering', () => {
  beforeEach(() => {
    render(<ByDateTab />);
  });
  xtest('select log files button renders', () => {

  });
  xtest('calendar inputs render', () => {

  });
  xtest('select all files button renders', () => {

  });
  xtest('clear all selections button renders', () => {

  });
  xtest('inglest button renders', () => {

  });
});
